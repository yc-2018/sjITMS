import { formatMessage } from 'umi/locale';

export const binViewLocale = {
  rfbinviewBinFormat: formatMessage({ id: 'binView.binFormat' }),
  rfbinviewBinFormatAdd: formatMessage({ id: 'binView.binFormat.add' }),
  
}