import { formatMessage } from 'umi/locale';
export const stockOutConfigLocale = {
  title: formatMessage({ id: 'stockOutConfig.title' }),
  stockAllocateType: formatMessage({ id: 'stockOutConfig.stockAllocateType' }),
  stockAllocateScheme: formatMessage({ id: 'stockOutConfig.stockAllocateScheme' }),
}