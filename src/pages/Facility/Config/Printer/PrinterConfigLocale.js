import { formatMessage } from 'umi/locale';

export const printerConfigLocale = {
  printAddress: formatMessage({ id: 'printerConfig.printAddress' }),
  getPrinter: formatMessage({ id: 'printerConfig.getPrinter' }),
  shortName: formatMessage({ id: 'printerConfig.shortName' }),
}