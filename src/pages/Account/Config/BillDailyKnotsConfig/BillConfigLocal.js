import { formatMessage } from 'umi/locale';

export const billDailyLocal = {
  billFormat: formatMessage({ id: 'bill.maxCount' }),
  billFormatAdd: formatMessage({ id: 'binView.binFormat.add' }),
  dailyFormat: formatMessage({ id: 'company.daily.time' }),
  billConfigFormat: formatMessage({ id: 'bill.config' }),
  dailyConfigFormat: formatMessage({ id: 'daily.config' }),

}
