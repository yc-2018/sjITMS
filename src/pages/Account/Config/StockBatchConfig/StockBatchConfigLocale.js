import { formatMessage } from 'umi/locale';

export const stockBatchConfigLocale = {
  configInfo: formatMessage({ id: 'stockBatch.configInfo' }),
  dateLength: formatMessage({ id: 'stockBatch.dateLength' }),
  flowLength: formatMessage({ id: 'stockBatch.flowLength' }),
  operateInfo: formatMessage({ id: 'stockBatch.operate.info' }),
}
